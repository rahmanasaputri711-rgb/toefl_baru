<div class="form-section">
    <div class="fs-title"><i class="fas fa-lightbulb" style="color:var(--gold)"></i>
        Pembahasan / Rationale
        <small style="color:var(--muted);font-weight:400;text-transform:none">— hanya tampil untuk admin</small>
    </div>
    <textarea name="pembahasan" class="form-control" rows="2"
        placeholder="Jelaskan mengapa jawaban ini benar dan yang lain salah...">{{ old('pembahasan') }}</textarea>
</div>
